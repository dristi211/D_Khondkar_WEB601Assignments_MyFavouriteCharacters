import { ContentTypeFilterPipePipe } from './content-type-filter-pipe.pipe';

describe('ContentTypeFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ContentTypeFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
